from datetime import datetime, date

def chkDateTime(devIP, uName, passWord):
    print("checkin the status...")
    return status

def fetchUserGroup(uName):
    f = open ("ugroup", "r")
    for i in f:
        print(i)
